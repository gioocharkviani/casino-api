export enum TransactionType {
  BET = 'bet',
  WIN = 'win',
  DEPOSIT = 'deposit',
  DEBIT = 'debit',
  WITHDRAWAL = 'withdrawal',
  BONUS = 'bonus',
  ROLLBACK = 'rollback',
  ADJUSTMENT = 'adjustment',
}
